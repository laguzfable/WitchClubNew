# 變更一覽 — 2026-08-25

這一輪對話動過的東西。

## 1. 作弊名字

chapter0 取名時輸入 **洛蘭** → 回憶模式全 CG、怪物圖鑑、蝕之聖典、女巫競技場全開。
結局和符文不碰（會污染成就）。換名字改 `CheatUnlock.cs` 的 `CheatNames`。

`CheatUnlock.cs`（新）、`CheatUnlockDebugMenu.cs`（新）、`PlayerNameStore` `VisitedNodeManager` `TitleMenuUnlockInjector`

## 2. 每個選項都給好感

77 個選項補上 `+5`，對象看那個選項在跟誰互動。結局判定或沒有好感對象的選項刻意不給。
順手修：`yellow01~03` 各兩處 `goto:.ritual` 少了 `@`，一直在噴紅字。

⚠️ `chapter3.nani` 開頭把五個好感歸零，序章～第二章的好感會被清光。**未處理。**

## 3. 地圖事件預約

`@overrideEvent` 本來只活在記憶體：關遊戲會掉、回標題卻不會消失（上一輪的涅莉引導綁架下一輪）。
改成寫 PlayerPrefs，並在 `@exitToTitle`／跳節點／F10 三處清掉。

`MapSpecialOverride` `MapSpecialOverrideData` `OverrideEventCommand` `ExitToTitleCommand` `ProgressResetter`

## 4. 蝕之聖典：星塵開場 → 選女角 → 進節點

點節點 → 星塵講話（Naninovel 對話框 + `stardust1`）→ 選一人 → 進節點。
**規則：選了誰，誰就 100，其他人 0。** 「都不選」回到原本的走過判定；節點 `variableOverrides` 永遠最大，被寫死的角色不進面板。

會問的八格：SEANCE PARTING COMMUNION WHISPER TRUCE ACCOMPLICE REVERIE VIGIL
（判準：之後還會走到地圖日。第一～三章會被歸零，結局段沒有地圖日，都不問。）

立繪未拉，目前用代表色色塊佔位，流程可完整測。
順手清掉：`chapter4:699` 的過期註解、PARTING 的 `affinity_Ved=0`（Ved≥50 判定早就不存在了）。

`AffinityChoicePanel.cs`（新）、`BranchMapUI` `BranchDatabase` `NodeButton` `MyBranchDB`

## 5. 西碧兒不再被儀式門檻擋住

她那五場沒有 `@battle`，是純劇情養成。掛在 `RitualGate` 上導致：門檻擋死（要 ≥20，實際只有 +5）＋進度永遠不推進（等一場不存在的戰鬥）。從表裡拿掉。

修完數字剛好對上：5 → 15 → 30 → 50，對應 `動手≥30 → badend13`、`收手≥50 → syb_end`。

## 6. 完成度與結局提示

完成度（`結局 N / 20`）本來就寫好，只是 prefab 欄位沒接；改成沒接就自動生一個。
右頁滑到節點顯示「這一格通往的結局」，拿過顯示名字、沒拿顯示 `？？？`。

| 節點 | 結局 |
|---|---|
| SEANCE | 14 壞滅、16 魔蝕 |
| PARTING | 17 蘇生、18 終焉 |
| COMMUNION | 15 肅清 |
| WHISPER | 05 深春、06 火中の幻影、13 唯一、20 私奔 |
| TRUCE | 12 異端 |
| VIGIL | 10 改寫命運、11 殉道 |
| CROWN | 01 緋紅替身、02 純藍之冠 |
| CHALICE | 03 在妳身邊、04 穢血新神、19 墮星之主 |
| SPIRAL | 07 輪迴の鑰匙、08 背棄世界、09 小精靈，飛走了 |

`EndingCatalog.cs`（新，名字目前只有中文）

---

## 待辦

1. **虛擬符文** — 聖典真正的門檻是符文不是好感，發好感打不開分歧，PARTING 跳進去四條路仍全撲空。提議：記下這一輪選的顏色，讓 `RunesXXX()` 回 `Max(真實, 選到的?5:0)`，不寫 PlayerPrefs。**等你測完定方向。**
2. `chapter3` 開頭的好感歸零
3. 選人面板的立繪（等切圖）
4. `chat_sybil.nani` 目前沒有入口
5. 第二輪繼承好感（跟聖典是兩件事）

## 待查

- **chapter2 選書選項重複三份** — 選項在 250 行、`@stop` 也在，不該溢出到 535 行的對白。要重現方式。
- `@choice` 碰到半形空格就截斷文字（「奧秘之書《無窮魔源》(The」）。舊問題。
