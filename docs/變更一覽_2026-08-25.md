# 變更一覽 — 2026-08-25

這一輪對話動過的東西。只列這次改的，之前留在工作區的修改不在裡面。

---

## 1. 作弊名字（全解鎖）

在 chapter0 取名時輸入指定名字，把收集要素一次打開。目前的名字是 **洛蘭**。

| 檔案 | 動作 |
|---|---|
| `Assets/Scripts/CheatUnlock.cs` | 新增。核心邏輯 |
| `Assets/Scripts/Editor/CheatUnlockDebugMenu.cs` | 新增。`Tools > Witch Club > 作弊名字` |
| `Assets/Scripts/CustomNaniCommand/PlayerNameStore.cs` | `Save()` 時比對名字 |
| `Assets/newsystems/VisitedNodeManager.cs` | `IsVisited` 在作弊模式一律回 true |
| `Assets/Scripts/UI/TitleMenuUnlockInjector.cs` | 兩顆隱藏按鈕加上 `|| CheatUnlock.IsActive` |

**要換名字**：改 `CheatUnlock.cs` 開頭的 `CheatNames` 陣列（可放多個，比對會 Trim + 忽略大小寫）。

**打開**：回憶模式全 CG、怪物圖鑑、蝕之聖典（按鈕＋全節點）、女巫競技場。
**不打開**：結局紀錄、符文、卡片解鎖 —— 那些要自己跑。`EndingRecord` 直接餵成就判定，灌假資料會污染 Steam 成就。

狀態存在 PlayerPrefs `WC/CheatUnlock/v1`，F10 清進度會一起關掉。

---

## 2. 每個選項都給好感

77 個選項補上 `set:affinity_XXX=affinity_XXX+5`，對象看該選項實際互動的是誰。

- **路線篇**（blue／red／green／yellow01-05）：所有選項 → 該線角色
- **主線雙選項**：兩邊都給，各自對應說話的人
- **單顆推進用選項**：也給（例：chapter2「就是說嘛」→ 梅爾）
- **選書那三顆**：保留 `bossPower`，用 `;` 串起來

**刻意沒給的**：結局判定或無好感對象的選項 —— chapter5common 米夏夢境、chapter4green 動手／收手（它在讀 `affinity_Syb>=30/50`，加了會翻掉門檻）、chapter5blue 776/777 與 838/839、chapter5yellow 236/237、chapter6red 兩組、chapter4red「就地解決」、chapter0 名字確認。

⚠️ **`chapter3.nani` 開頭第 2–6 行把五個好感全部歸零**，所以序章～第二章新加的好感進第三章會被清光。這件事還沒處理，等你決定。

順手修的舊 bug：`yellow01/02/03.nani` 各有兩處 `goto:.ritual` 少了 `@`，被當成 `@print` 解析，Console 一直噴紅字。已改成 `@goto .ritual`。

---

## 3. 地圖特殊事件預約（`@overrideEvent`）

原本是純記憶體的 static dictionary，兩個方向都會出錯：**存檔關遊戲會掉**、**回標題重開卻不會消失**（上一輪排好的涅莉引導會綁架下一輪的第一個地圖日）。

| 檔案 | 動作 |
|---|---|
| `Assets/MapSystem/MapSpecialOverride.cs` | 改寫。寫進 PlayerPrefs `WC/MapSpecialOverride/v1`，新增 `ClearAll()` |
| `Assets/MapSystem/MapSpecialOverrideData.cs` | 新增 `animatorPath` 欄位 |
| `Assets/Scripts/CustomNaniCommand/OverrideEventCommand.cs` | 記下動畫的 Resources 路徑 |
| `Assets/Scripts/CustomNaniCommand/ExitToTitleCommand.cs` | `ResetStateAsync()` 之後 `ClearAll()` |
| `Assets/newsystems/NodeButton.cs` | 跳節點時 `ClearAll()` |
| `Assets/newsystems/ProgressResetter.cs` | F10 時 `ClearAll()` |

動畫是資產參照、序列化不進 PlayerPrefs，所以存路徑、讀回來再 `Resources.Load`。載不到只印 warning，事件照播。

---

## 4. 蝕之聖典：星塵開場 → 選女角 → 進節點

聖典是給玩家收結局用的，但好感不夠就做不了夜晚儀式 → 沒符文 → 走不到分歧，等於要玩家把養成再做一遍。偷偷灌滿不行（會被研究出來），所以**明著問**，而且讓星塵來問 —— 它本來就是操弄記憶與輪迴的角色。

| 檔案 | 動作 |
|---|---|
| `Assets/newsystems/AffinityChoicePanel.cs` | 新增。整個面板 |
| `Assets/newsystems/BranchMapUI.cs` | 新增星塵台詞、選項清單等 Inspector 欄位 |
| `Assets/newsystems/BranchDatabase.cs` | `BranchNode` 新增 `askAffinity`、`stardustLine`、`endingIds` |
| `Assets/newsystems/NodeButton.cs` | 拆成 `OnNodeClick` → 面板 → `Enter(chosen)` |
| `Assets/newsystems/MyBranchDB.asset` | 15 格逐格設定 |

**流程**：點節點 → 收起書頁 → 星塵講話（走 Naninovel 的真對話框、`stardust1` 背景、`@char 星塵`）→ 淡入四張立繪 → 選一個進節點。

**好感規則**（玩家看得懂的一句話）：**選了誰，誰就 100，其他人 0。**
- 按「都不選」→ 回到原本的「走過那條線就發滿」判定
- 節點自己的 `variableOverrides` 永遠排最後、最大
- 被 override 寫死的角色不會出現在面板上（留著等於騙玩家）

**哪幾格會問**（判準：這一格之後還會走到地圖日）：
SEANCE、PARTING、COMMUNION、WHISPER、TRUCE、ACCOMPLICE、REVERIE、VIGIL。
不問：HOUND／TOME／TETHER（chapter3 開頭會歸零）、UNMASK／CHALICE／CROWN／SPIRAL（結局段，沒有地圖日）。

**立繪還沒拉**：目前用四條線的代表色畫色塊佔位，流程可以完整測。圖進 Unity 後（Sprite Mode: Multiple，切四張）拖進 BranchMapUI 的 `Affinity Choice Options` → `Portrait` 即可。

順手清掉的過期設定：`chapter4.nani:699` 那行「綠線是 Ved>=50 自動判定」是舊制度的殘留註解（現在四條路一律看符文數），PARTING 那格為它加的 `affinity_Ved=0` 也拿掉了，薇狄亞回到面板上。

---

## 5. 西碧兒不再被儀式門檻擋住

`Assets/MapSystem/RitualGate.cs` —— 把 Sybil 從表裡拿掉。

她那五場 `syb_day01~05` 一個 `@battle` 都沒有，是純劇情養成、不給符文。掛在 RitualGate 上造成兩個 bug：

1. 第一場要好感 ≥20，但玩家進綠線時只有 +5 → 三個地圖夜全被換成閒聊，`syb_day01` 一次都見不到
2. `HasGate` 為 true → 點了不推進進度，要等戰鬥勝利，但 `CharacterOf` 不認得她 → 進度永遠 0

修完數字剛好對上（這也是原設計的證據）：告訴西碧兒 +5 → 15 → 30 → 50，對應 `動手 ≥30 → badend13`、`收手 ≥50 → syb_end.end1`。

> `chat_sybil.nani` 現在沒有入口了（它本來是門檻不足時的替代品），先留著。

---

## 6. 聖典的完成度與結局提示

| 檔案 | 動作 |
|---|---|
| `Assets/newsystems/EndingCatalog.cs` | 新增。20 個結局 id → 名字 |
| `Assets/newsystems/BranchMapUI.cs` | 完成度自動生 Text；右頁多一段結局提示 |
| `Assets/newsystems/MyBranchDB.asset` | 15 格填好 `endingIds` |

完成度（`結局 N / 20`）本來就寫好了，只是 prefab 的 `completionText` 是空的、程式直接 return。現在欄位空著會自動生一個，位置用 `Completion Auto Anchor` 調；你之後做好自己的 Text 拖進欄位就會優先用你的。

右頁滑到節點時顯示「這一格通往的結局」：已收集顯示名字，沒拿到顯示 `？？？`。

**節點 → 結局對照**（每個結局只掛在它真正分歧的那一格）：

| 節點 | 結局 |
|---|---|
| SEANCE | 14 壞滅、16 魔蝕 |
| PARTING | 17 蘇生、18 終焉 |
| COMMUNION | 15 肅清 |
| WHISPER | 05 深春、06 火中の幻影、13 唯一、20 私奔 |
| TRUCE | 12 異端 |
| VIGIL | 10 改寫命運、11 殉道 |
| CROWN | 01 緋紅替身、02 純藍之冠 |
| CHALICE | 03 在妳身邊、04 穢血新神、19 墮星之主 |
| SPIRAL | 07 輪迴の鑰匙、08 背棄世界、09 小精靈，飛走了 |

結局名字目前只有中文，寫死在 `EndingCatalog`。不跟 Steam 拿是因為成就名稱要連線才有，聖典離線也要能看。要多語言的話把那張表換成查 managed text，介面不用動。

---

## 還沒做 / 等你決定

1. **虛擬符文** —— 聖典的真正門檻是符文（`RunesGreen()>=3` 那些），不是好感。發好感打不開分歧，從 PARTING 跳進去四條路仍然全部撲空 → 失敗終焉。提議：記下「這一輪選的顏色」，讓 `RunesXXX()` 回傳 `Max(真實, 選到的?5:0)`，不寫進 PlayerPrefs，所以符文圖鑑／牌組／成就都不受影響、符文還是要自己跑。**你說測完再定方向。**
2. **chapter3 開頭的好感歸零** —— 序章～第二章的好感會被清掉。
3. **選人面板的立繪** —— 等你切圖。
4. **`chat_sybil.nani`** —— 目前沒有入口。
5. **第二輪繼承好感** —— 跟聖典是兩件事，還沒做。

## 待觀察

- **chapter2 選書那段出現重複三份的選項**（截圖那個），成因還沒找到。選項在第 250 行、`@stop` 也在，正常不該溢出到第 535 行的對白。要重現的話告訴我當時的操作（有沒有按 SKIP／回捲）。
- 那三顆選項的文字被截成「奧秘之書《無窮魔源》(The」—— `@choice` 的無名參數碰到半形空格就截斷。舊問題，要修跟我說。
